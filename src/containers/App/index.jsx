import React from 'react';

import './index.scss';

const App = () => (
  <div className='App'>
    add new components or HTML here by importing them and then applying as a JSX tag  
  </div>
);

export default App;
